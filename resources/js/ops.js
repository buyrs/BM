// Ops-specific JavaScript
import Alpine from 'alpinejs';

// Initialize Alpine
window.Alpine = Alpine;
Alpine.start();

// Ops-specific functionality
document.addEventListener('DOMContentLoaded', function() {
    console.log('Ops scripts loaded');
    
    // Add any ops-specific scripts here
    // For example, mission management, property assignment, etc.
});